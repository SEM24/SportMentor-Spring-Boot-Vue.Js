server:
  port: 8761
spring:
  application:
    name: eureka-server
eureka:
  server:
    renewal-percent-threshold: 0.49
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false